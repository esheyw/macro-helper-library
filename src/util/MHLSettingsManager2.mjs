/**
 * @typedef {object} MHLSettingsManagerOptions
 * @property {boolean} [associateLabels=true] Whether to associate the text labels of settings with their form input element
 * @property {string} [choiceInfix="Choice"]
 */

export class MHLSettingsManager2 {
  /**
   * 
   * @param {string|BaseModule} moduleID 
   * @param {*} options 
   */
  constructor(moduleID, options={}) {

  }
}